# kb-loader
Document ingestion and FAISS indexing tool.

### Steps
1. Place your `.pdf` or `.docx` files inside `data/`.
2. Add your OpenAI API key in environment variable `OPENAI_API_KEY`.
3. Run:
   ```bash
   python src/main.py
   ```
